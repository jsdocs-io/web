import { packageAnalyzer } from '../../src/lib/package-analyzer';

describe('PackageAnalyzer instance', () => {
    it('should be defined', () => {
        expect(packageAnalyzer).toBeDefined();
    });
});
