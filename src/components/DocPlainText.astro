---
import type { DocPlainText as _DocPlainText } from "@microsoft/tsdoc";
import { mdToHtml } from "../../lib/md-to-html";

type Props = {
	plainText: _DocPlainText;
};

const {
	plainText: { text },
} = Astro.props;
const html = await mdToHtml(text);
---

<Fragment set:html={html} />
