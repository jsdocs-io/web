---
import { type DocCodeSpan as _DocCodeSpan } from "@microsoft/tsdoc";

type Props = {
	codeSpan: _DocCodeSpan;
};

const {
	codeSpan: { code },
} = Astro.props;
---

<code class="text-nowrap">{code}</code>
